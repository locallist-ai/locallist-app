// Re-export from design system (extraído en feat/design-system-fase-0).
// Mantenemos este archivo para no romper imports existentes como
// `import { ProgressDots } from '../../components/home/ProgressDots'`.
export { ProgressDots } from '../ui/design-system';
